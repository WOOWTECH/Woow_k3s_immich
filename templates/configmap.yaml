---
apiVersion: v1
kind: ConfigMap
metadata:
  name: immich-config
  namespace: {{ include "immich.ns" . }}
  labels:
    app: immich
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
